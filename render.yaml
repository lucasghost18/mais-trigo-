services:
  - type: web
    name: mais-trigo
    env: python
    plan: free
    region: oregon
    rootDir: mais-trigo--feature-update-readme
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mais-trigo-db
          property: connectionString
      - key: PRINTER_OUTPUT_DIR
        value: /tmp/prints
    disk:
      name: mais-trigo-data
      mountPath: /opt/render/project/src/instance
      sizeGB: 1

  - type: postgres
    name: mais-trigo-db
    plan: free
    region: oregon
    ipAllowList: []

